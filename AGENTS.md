# AGENTS.md

## 핵심 구현 원칙

* 가장 단순하면서 정확한 구현을 우선한다.
* 문제의 증상을 가리는 workaround보다 root cause를 수정한다.
* 특정 케이스를 통과시키기 위한 끼워맞추기식 조건문이나 패치성 로직을 추가하지 않는다.
* 실제 요구사항이 없는 fallback, defensive logic, 예외 처리를 추가하지 않는다.
* 필요 이상의 abstraction, wrapper, helper, layer를 만들지 않는다.
* 기존 구조를 단순화해서 해결할 수 있다면 새로운 로직을 추가하지 않는다.
* 현재 요구되지 않은 범용성이나 미래 확장성을 미리 구현하지 않는다.
* 여러 구현이 가능하면 moving parts가 가장 적은 구조를 선택한다.
* 코드는 compact하게 유지하되 정확성, 가독성, 안정성을 희생하지 않는다.

## 수정 원칙

* 기존 동작을 이해한 뒤 수정한다.
* 수정 범위는 문제 해결에 필요한 최소 범위로 제한한다.
* unrelated refactoring을 함께 수행하지 않는다.
* 같은 문제를 여러 위치에서 각각 보정하지 말고 가능한 한 한 곳에서 해결한다.
* 새로운 상태값이나 플래그를 추가하기 전에 기존 상태 모델로 해결할 수 있는지 확인한다.

## 검증

* 변경 후 관련 테스트를 실행한다.
* 테스트를 통과시키기 위해 production logic을 왜곡하지 않는다.
* 테스트 실패 시 테스트 자체가 잘못된 것인지 구현이 잘못된 것인지 먼저 판단한다.
